"use client";
import { useState } from "react";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { services } from "@/lib/company";
export function QuoteForm(){const [sent,setSent]=useState(false);if(sent)return <div className="border border-signal/30 bg-signal/10 p-10 text-center"><CheckCircle2 className="mx-auto text-signal" size={50}/><h2 className="mt-5 font-display text-4xl uppercase">Request captured</h2><p className="mt-3 text-white/55">This demo form is ready to connect to the company email or CRM once verified contact details are provided.</p><button className="mt-6 text-xs font-bold uppercase tracking-widest text-signal" onClick={()=>setSent(false)}>Submit another</button></div>;return <form onSubmit={e=>{e.preventDefault();setSent(true)}} className="grid gap-5 sm:grid-cols-2"><label className="text-xs font-bold uppercase tracking-wider text-white/55">Full name *<input required name="name" className="field mt-2" placeholder="Your name"/></label><label className="text-xs font-bold uppercase tracking-wider text-white/55">Phone number *<input required name="phone" type="tel" className="field mt-2" placeholder="Your phone"/></label><label className="text-xs font-bold uppercase tracking-wider text-white/55">Email<input name="email" type="email" className="field mt-2" placeholder="you@example.com"/></label><label className="text-xs font-bold uppercase tracking-wider text-white/55">Service *<select required name="service" className="field mt-2"><option value="">Select a service</option>{services.map(s=><option key={s.title}>{s.title}</option>)}</select></label><label className="text-xs font-bold uppercase tracking-wider text-white/55 sm:col-span-2">Site location *<input required name="location" className="field mt-2" placeholder="City / project location"/></label><label className="text-xs font-bold uppercase tracking-wider text-white/55 sm:col-span-2">Project details<textarea name="details" rows={5} className="field mt-2 resize-none" placeholder="Tell us about your requirement, site and preferred timeline"/></label><Button type="submit" size="lg" className="sm:col-span-2 sm:justify-self-start">Send request <ArrowRight size={17}/></Button></form>}
