"use client";
import { useState } from "react";
import { QuotationPrintDocument } from "./QuotationPrintDocument";
import { getValidItems, type QuotationState } from "./quotation-model";
import "./quotation.css";
import "./quotation-refinement.css";
export function SavedQuotationPdf({quotation}:{quotation:QuotationState}){const [message,setMessage]=useState("");const valid=getValidItems(quotation.items);const canPrint=Boolean(quotation.client.companyName.trim()&&quotation.serviceType.trim()&&quotation.subject.trim()&&valid.length>0);const print=async()=>{if(!canPrint){setMessage("Complete the client, service, subject, and at least one valid price item before generating the PDF.");return;}setMessage("");const started=Date.now();while(document.querySelector('.quotation-print-root')?.getAttribute('data-print-ready')!=="true"&&Date.now()-started<5000)await new Promise(resolve=>setTimeout(resolve,50));if(document.querySelector('.quotation-print-root')?.getAttribute('data-print-ready')!=="true"){setMessage("The quotation document could not be prepared for printing. Please try again.");return;}window.print();};return <><div className="saved-pdf-screen"><button onClick={print} className="border border-signal bg-signal px-5 py-3 text-xs font-bold uppercase tracking-wider text-black">GENERATE / SAVE PDF</button>{message&&<p className="mt-3 max-w-sm text-sm text-red-300">{message}</p>}</div><QuotationPrintDocument quotation={quotation}/></>}
